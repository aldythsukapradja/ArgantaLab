{
  "name": "arganta-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Claude Code bridge — drives the Agent SDK from HQ agent chat over a token-gated localhost WebSocket.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.212",
    "dotenv": "^17.4.2",
    "pg": "^8.22.0",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/ws": "^8.18.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
