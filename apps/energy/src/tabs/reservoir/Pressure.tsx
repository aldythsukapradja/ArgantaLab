// Pressure.tsx — placeholder pending its full build (see RESERVOIR-MANAGEMENT-PLAN.md).
import { Page, TabHeader } from './surface';
export function Pressure() {
  return (
    <Page>
      <TabHeader title="Pressure Evolution" subtitle="Coming online in this build pass." />
      <div style={{ padding: 40, textAlign: 'center', color: 'var(--muted)', fontSize: 12 }}>Surface under construction.</div>
    </Page>
  );
}
