# M3 UI Build — Knowledge OS (drawer shell · vault · extraction studio · Cosmonaut)

Date: 2026-07-21 · Executor: Opus · Spec: `docs/arganta-energy/M3-SPEC.md` (LOCKED v1.0.0)

## What shipped

### 1 · Shell rework (§1)
- **`src/components/Drawer.tsx`** — new MAIN nav replacing the icon `ActivityRail`. Expanded 232px by default, collapsible to 60px icon rail (chevron, persisted `ae_drawer`). MOTHERSHIP / VERTICALS group labels derived from `nav.ts` zones; locked sibling apps at the bottom. Each item icon + label + phase chip (chip → tooltip when collapsed).
- **`src/components/ContextBar.tsx`** — reworked into the SUB-TAB bar: domain label + sub-tabs + well selector + ⌘K + theme toggle + route badge. Sub-tabs render from the new `SUBTABS: Record<DomainId, SubTab[]>` config in `nav.ts`.
- **`src/components/MobileBar.tsx`** — new. ≤820px bottom tab bar (Core · Data · Knowledge · center-raised Cosmonaut orb · Field Dev) + "⋯ More" sheet listing all 10 domains. Sub-tabs become a horizontally scrollable strip.
- **`src/App.tsx`** — responsive shell (`useIsMobile` at 820px), routes domain + subtab; StatusBar (desktop) / MobileBar (mobile); floating orb + Cosmonaut overlay + CommandPalette mounted.
- **`src/store.ts`** — added `subtab` (reset by `setDomain`), `drawerCollapsed`, `cosmoOpen`, `userNotes` (extraction layer), `selectedNoteId`, `goto`, `openNote`, `addUserNote`.
- **CommandPalette** now lists domains + sub-tabs and jumps via `goto`.

### 2 · Sub-tabs (§2)
foundation → Overview (Foundation) · Schema (SchemaTab); data → Inventory (DataTab) · Pipeline (new `DataPipeline.tsx`, mirror→decode→validate stage cards with real counts); knowledge → Explorer · Graph · Extraction; others → single overview.

### 3 · Knowledge surface (§3)
- **`src/knowledge/vault.ts`** — merges baked `kb.json` ▸ user layer (`localStorage ae_kb_user`), runs `recomputeLinks` over the union, resolves evidence source_ids → sha256 via the `data.json` ledger.
- **`src/tabs/knowledge/Explorer.tsx`** — three-pane: folder tree + search + count badges · rendered markdown with clickable wikilinks / `.wikilink.dead` · frontmatter chips + evidence (sha256 reveal) + backlinks + claims.
- **`src/tabs/knowledge/Graph.tsx`** — Canvas2D graph over `toGraph`: color by type, radius ∝ degree, seeded radial-by-type-ring layout + relax iterations, hover neighbor-highlight, click→open note, pan/zoom, rings/galaxy switcher, rAF-gated, static under reduced-motion.
- **`src/tabs/md.tsx`** — upgraded: wikilink resolution (clickable / dead) against a note set + `onOpenNote`, blockquote callouts.
- **Extraction Studio** — `src/knowledge/extract.ts` (PDF via pdfjs `getTextContent`; XLSX/CSV via SheetJS `sheet_to_json` real tables; DOCX/PPTX via jszip + DOMParser, slides sorted NUMERICALLY; TXT paragraphs; sha256 via WebCrypto) + `src/knowledge/tag.ts` (three-tier: labelled key:value regexes · wellbore shape patterns via `normalizeWellbore` + 16 surface names · evidence windows `slice(i-110,i+170)` max 3; matches resolve to merged KB, unresolved → `noteId:null` fuzzy) + `src/tabs/knowledge/Extraction.tsx` (three-pane studio: source queue+dropzone | flow-line + candidate cards | inspector accept/reject + batch log). Accept → user-layer VaultNote (`gen:'extract'`, `type:'extracted'`, folder `05 Documents`, evidence `upload:<sha256>#<locator>`, embedded `[[wikilinks]]`) → visible in Explorer + Graph immediately. All deps lazy-loaded (dynamic import); pdfjs worker via `?url`.

### 4 · Cosmonaut (§4)
- **`src/components/Cosmonaut.tsx`** — layered CSS orb (halo pulse / conic spin + reverse / glass core breathe / hover ring + label pill; all animation off under reduced-motion) floating bottom-right (desktop) + center-raised (mobile bar). Full-screen 3-pane overlay: LEFT history (`localStorage ae_cosmo_sessions`) | CENTER chat (max-width stream, suggestion chips, typewriter reveal) | RIGHT artifact drawer (SVG chart / table / md); animated grid-template-columns; drawers → sheets on mobile; Esc closes. Truthful trace + dataNature badges per reply; DET·SOV·FRO tier rack with SOV/FRO locked ("upgrade seam — not wired").
- **`src/cosmo/router.ts`** — deterministic (tier 0A, no LLM) grounded to `foundation.json` + schema-meta FKS + `kb.json`: production summary (+SVG bar chart), well coverage (table), schema/relationships (FK table), surfaces/tops, knowledge stats, fallback capability card.

## Verification
- `tsc --noEmit` clean (strict); `npm run build` green (pdf/xlsx/jszip emitted as separate lazy chunks — base bundle unaffected).
- Browser (port 5279), desktop 1440×900 + mobile 375×812, both themes, zero console errors.
- Extraction proven END-TO-END with 5 synthetic test files (txt/csv/xlsx/pptx/pdf): PDF parsed via pdfjs (1 block/4 cand), PPTX 2 slides via jszip, XLSX real table via SheetJS, all deterministic. Accepted the TXT doc-note (4/4 entities resolved) → vault grew 77→78, note appears in Explorer tree + as a backlink on "Hugin Fm. VOLVE Top". Test note was synthetic and clearly marked; only one accepted to prove the flow.
- Wikilink click-through, graph interactions (rings/galaxy, hover, zoom), mobile bottom bar + More sheet, Cosmonaut 4 intents (production/schema/surfaces/coverage) with artifacts + traces + all four nature badges — all confirmed.

## Deviations
- **Foundation `schema` sub-tab** renders the existing `SchemaTab` (semantic model) rather than moving `SchemaCanvas` — the spec marked this optional ("keep Schema domain too for now"); reused the built surface to avoid duplicating the canvas.
- **Coverage artifact** columns are Production + Trajectory (the two per-wellbore datasets present in `foundation.json`); logs/picks are not in the foundation payload, so they were omitted rather than fabricated (truth rule). "—" marks honest gaps.
- Added `src/vite-env.d.ts` (`/// <reference types="vite/client" />`) so the `?url` worker import type-checks. No contract/data/script files were touched.
