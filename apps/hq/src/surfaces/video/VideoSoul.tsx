// V0 shell for the Soul tab (V2 builds this out: character cards, keyframe
// forge, style presets, identity test matrix). Wires the LoRA per plan V2.
import { UserRound } from 'lucide-react'

export function VideoSoul() {
  return (
    <div className="vs-shell">
      <UserRound size={26} />
      <p>Soul ID — character identity management lands here (V2): keyframe forge, style presets, identity test matrix, and the ARGANTA character LoRA.</p>
    </div>
  )
}
