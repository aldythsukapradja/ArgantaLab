VITE_SUPABASE_URL=https://YOURREF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
# Optional: absolute base URL for the client art library (e.g. Supabase
# Storage public bucket). Defaults to /data (vite dev middleware / local).
VITE_DATA_BASE=
