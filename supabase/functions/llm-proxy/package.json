{
  "name": "llm-proxy-router",
  "private": true,
  "type": "module",
  "description": "Pure routing/pricing/translation logic for the llm-proxy Edge Function, split out so it's testable under plain Node (Deno runs index.ts, which imports router.js directly)."
}
