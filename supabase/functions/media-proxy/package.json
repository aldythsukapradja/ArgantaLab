{
  "name": "media-proxy-router",
  "private": true,
  "type": "module",
  "description": "Pure routing/translation/pricing for the media-proxy Edge Function, split out so it's testable under plain Node (Deno runs index.ts, which imports router.js)."
}
