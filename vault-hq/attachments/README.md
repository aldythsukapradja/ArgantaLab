# attachments

Images, screenshots, and pasted media referenced by notes live here — inside the vault —
so links survive a copy-paste, a new Obsidian, or a dedicated repo.

Reference them with `![[filename.png]]`. Name them after the note that uses them.

Keeping media *inside* the folder is part of the One-Folder Principle (see [[README]]).
