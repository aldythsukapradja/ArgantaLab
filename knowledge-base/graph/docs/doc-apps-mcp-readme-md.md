---
title: The Bridge — an LLM seat over Circle HQ
type: doc-node
product: HQ
status: current
verdict: current
tags: [doc, atlas]
date: 2026-07-11
---

# The Bridge — an LLM seat over Circle HQ

`apps/mcp/README.md` · verdict **current**

Every claim verified in code: apps/mcp/src/tools.ts registers all documented tools plus more, render.yaml deploys it, and the reuse of apps/hq/src/data/graph/* is real. KB §10 confirms 'Bridge live on Render'. README only documents 9 core tools in its table while the server actually exposes 20 (adds valuation_* and pixel_* groups) — a doc-lag undersell, not a superseded approach.

**Lesson:** Provenance-badging every value (live/partial/simulated/placeholder) is the proven instinct that keeps the cockpit honest — but it also exposes that the Bridge is a truthful dashboard over an empty room; the 'What's next' live-Supabase source is the unbuilt link from simulated to measured (debt D8, gated by D1 zero users).

In [[00-doc-atlas]] · product [[HQ]].
